<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uabc.amc.starbuzz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <!-- STORE -->
        <activity android:name=".activities.stores.StoreCategoryActivity"/>
        <activity android:name=".activities.stores.StoreActivity" />
        <!-- FOOD -->
        <activity
            android:name=".activities.foods.FoodActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.foods.FoodCategoryActivity" />
        <!-- DRINK -->
        <activity
            android:name=".activities.drinks.DrinkActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.drinks.DrinkCategoryActivity" />
        <!-- MAIN|TOP_ACTIVITY -->
        <activity android:name=".TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>